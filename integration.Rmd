## -*- mode: poly-markdown+R; -*-
---
title: "CG contexts"
output: html_document
params:
  seed: 11
---

## Set up
    
```{r settings, include=FALSE}

library(knitr)
library(devtools)
library(reshape2)
library(lattice)
## library(Hmisc)
library(pheatmap)

TASK <- "cg_context_bulk"
HOME <- '/home/imallona'
WD <-  file.path(HOME, TASK)
DATA <- file.path(HOME, 'data')

MIN_DEPTH <- 10
NFS <- file.path(HOME, 'mnt', 'nfs')


opts_chunk$set(fig.width = 12, fig.height = 4, fig.path = file.path(WD, TASK, 'RmdFigs'),
               output.dir = file.path(WD, TASK),
               warning = TRUE, message = FALSE)

dir.create(file.path(WD, TASK))
## setwd(file.path(NFS, TASK))

```

# Data import

## Session

```{r sessionInfo, include=TRUE, echo=TRUE, results='markup'}

sessionInfo()
devtools::session_info()

```
